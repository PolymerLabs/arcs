load("@npm_bazel_typescript//:index.bzl", "ts_config", "ts_library")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [":tsconfig-base"],
)

ts_config(
    name = "tsconfig-base",
    src = "config/tsconfig.base.json",
    deps = [],
)

ts_library(
    name = "arcs",
    srcs = glob(["src/**/*.ts"]),
    tsconfig = ":tsconfig",
    deps = [
        "@npm//@types",
    ],
)