# arcs

## Install
```
$ (cd runtime && npm install)
$ (cd tracelib && npm install)
```

## Test
```
$ (cd runtime && npm test)
```

## Trace
```
$ cd runtime
$ traceFile=`pwd`/trace.json npm test
```
