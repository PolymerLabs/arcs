meta
  namespace: arcs.src

particle Gold
  foo: writes Foo {txt: Text}
  cFoo: writes Foo {txt: Text, num: Number}
  bar: writes Bar {txt: Text}
  bar2: writes Bar {txt: Text}
  noName: writes {txt:Text}
  fooBar: writes Foo Bar {txt: Text}