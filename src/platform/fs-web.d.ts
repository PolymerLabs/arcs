// Export fs type definitions from Node. This module is not actually used in the web deployment, but it's useful to import the Node definitions
// to provide type information in an editor environment.
import fs from 'fs';
export {fs};
