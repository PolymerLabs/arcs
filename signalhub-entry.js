// This is transformed into signalhub.js by running:
// $ browserify -p tinyify signalhub-entry.js -o signalhub.js
// All these libraries (signalhub, browserify, tinyify) are available on npm.
window.signalhub = require('signalhub');
